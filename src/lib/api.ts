// // src/api/api.ts
// export const BASE_URL = 'https://kan-api.free.nf';

// export async function fetchFromAPI(endpoint: string) {
//   const res = await fetch(`${BASE_URL}/${endpoint}`);
//   if (!res.ok) {
//     throw new Error(`API error: ${res.status}`);
//   }
//   return res.json();
// }

// export const getDestinations = () => fetchFromAPI('destination');
